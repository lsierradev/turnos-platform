{
  "name": "@turnos-platform/tenant",
  "version": "0.0.1",
  "private": true,
  "description": "Contexto de taller por request y aislamiento con Row Level Security (Sprint 20).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.22",
    "@turnos-platform/auth": "workspace:*",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "typeorm": "^0.3.20"
  }
}
