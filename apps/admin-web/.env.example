VITE_API_URL=http://localhost:3001

# No hay login en este frontend todavia (fuera de alcance de Sprint 4).
# Este token es solo para desarrollo local: generarlo pegando el accessToken
# que devuelve POST /auth/login (usuarios-service, :3002) contra un usuario
# real, con rol acorde a lo que se quiera probar.
VITE_DEV_TOKEN=
