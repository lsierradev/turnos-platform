VITE_API_URL=http://localhost:3001
VITE_AUTH_URL=http://localhost:3002

# Desde Sprint 10 el panel tiene login propio: se entra con un usuario real
# contra POST /auth/login de usuarios-service. Ya NO existe VITE_DEV_TOKEN.
#
# Ojo: las dos variables de arriba se HORNEAN en el bundle en tiempo de
# build (asi funciona Vite), no se leen en runtime. Cambiarlas exige
# reconstruir la imagen -- ver docs/DESPLIEGUE.md.
