# packages

Código compartido entre `apps/` y `services/`: tipos/contratos comunes, utilidades, configuración (ESLint, TypeScript, etc.) y componentes de UI reutilizables.

Aún sin contenido — carpeta reservada para cuando se agreguen los primeros paquetes.
